spring:
  datasource:
    url: jdbc:tc:postgresql:11.17-alpine:///db
    hikari:
      schema: public
      minimum-idle: 1

pia:
  db-lock:
    create-tables: true
    lock-acquire-poll-interval: 100
    lock-acquire-timeout: 1000
    lock-hold-timeout: 2000
